# WSL2 configuration — copy this to /etc/wsl.conf on your Ubuntu machine
# Then restart WSL2 from PowerShell: wsl --shutdown
# After restart, systemd will be active and OpenClaw will auto-start

[boot]
systemd=true

[user]
default=kingseb
